sudo: required
services:
  - docker

before_install:
  - docker build -t jordanm/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true jordanm/react-test npm test

after_success:
  - docker build -t jordanm/multi-client ./client
  - docker build -t jordanm/multi-nginx ./nginx
  - docker build -t jordanm/multi-server ./server
  - docker build -t jordanm/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push jordanm/multi-client
  - docker push jordanm/multi-nginx
  - docker push jordanm/multi-server
  - docker push jordanm/multi-worker