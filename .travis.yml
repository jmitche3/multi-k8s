sudo: required
services:
  - docker

before_install:
  - docker build -t jordanmitchell146/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true jordanmitchell146/react-test npm test

after_success:
  - docker build -t jordanmitchell146/multi-client ./client
  - docker build -t jordanmitchell146/multi-nginx ./nginx
  - docker build -t jordanmitchell146/multi-server ./server
  - docker build -t jordanmitchell146/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push jordanmitchell146/multi-client
  - docker push jordanmitchell146/multi-nginx
  - docker push jordanmitchell146/multi-server
  - docker push jordanmitchell146/multi-worker